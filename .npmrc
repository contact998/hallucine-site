; Refine (@refinedev/core) déclare ses peer deps @types/react en ^17||^18.
; Le projet est sur React 19 → conflit de TYPES uniquement (runtime + build OK).
; Ce flag permet à npm install / npm ci de résoudre l'arbre (dev local ET build Railway).
legacy-peer-deps=true

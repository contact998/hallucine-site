export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";
export const getLoginUrl = () => "/login";
export const initLoginUrl = () => {
  // No-op: login URL is currently static. Placeholder for future dynamic OAuth config loading.
};
